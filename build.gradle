apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'HelloCodeAlpha'

repositories {
    mavenCentral()
}

dependencies {
    // no extra libraries needed
}

jar {
    manifest {
        attributes 'Main-Class': 'HelloCodeAlpha'
    }
}
